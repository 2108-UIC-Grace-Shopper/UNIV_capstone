import React from "react";
import ReactDOM from "react-dom";

const allProducts = () => {
    return (
        <div>

        </div>

    );
}
ReactDOM.render(<allProducts/>, document.getElementById("app"));